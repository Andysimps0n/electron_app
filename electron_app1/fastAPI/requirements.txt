# On Linux servers, plain "torch" installs the huge CUDA (GPU) build.
# Render machines have no GPU, so we pull the much smaller CPU-only wheel.
--extra-index-url https://download.pytorch.org/whl/cpu
torch

fastapi
uvicorn[standard]
transformers
